import{initialData}from'../data/initialData';const KEY='jusa-competitions-v1';const clone=x=>JSON.parse(JSON.stringify(x));
export const storageService={load(){try{return JSON.parse(localStorage.getItem(KEY))||clone(initialData)}catch{return clone(initialData)}},save(data){localStorage.setItem(KEY,JSON.stringify({...data,updatedAt:new Date().toISOString()}))},reset(){localStorage.removeItem(KEY);return clone(initialData)},export(data){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='jusa-dados.json';a.click();URL.revokeObjectURL(a.href)},async import(file){const d=JSON.parse(await file.text());if(!Array.isArray(d.competitions))throw Error('Arquivo inválido');return d}};
