import{useState}from'react';import{useNavigate}from'react-router-dom';import{LogIn}from'lucide-react';import{useCompetition}from'../context/CompetitionContext';import{supabaseService}from'../services/supabaseService';
const aliases={admin:'admin@jusa.app','arbitro-v2m':'arbitro.v2m@jusa.app','arbitro-v4x':'arbitro.v4x@jusa.app','arbitro-v2f':'arbitro.v2f@jusa.app','arbitro-fvm':'arbitro.fvm@jusa.app'};
export default function Login(){const{setUser,notify}=useCompetition(),navigate=useNavigate(),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget),raw=f.get('login').trim().toLowerCase(),email=aliases[raw]||raw;try{const user=await supabaseService.signIn(email,f.get('password'));setUser(user);notify('Acesso realizado');navigate(user.role==='admin'?'/admin':'/arbitragem')}catch{setError('Login ou senha incorretos.')}finally{setBusy(false)}};return <section className="loginpage"><form className="loginbox" onSubmit={submit}><div className="loginicon"><LogIn/></div><span>ACESSO DA ORGANIZAÇÃO</span><h1>Entrar no JUSA</h1><p>Use o acesso correspondente à sua modalidade.</p>{error&&<div className="formerror">{error}</div>}<label>Login ou e-mail<input name="login" autoComplete="username" required placeholder="arbitro-v2f"/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" disabled={busy}>{busy?'Entrando...':'Entrar'}</button></form></section>}
